class Solution {
    public int[] solution(int[] fees, String[] records) {
        int[] answer = {};
        for(int i=0;i<records.length;i++){
            String[]record = records[i].split(" ");
            if(record[2].equals("IN"))
        }
        int inTime = records[0];
        int outTime = record[1]
        int timeAmount =
        if()

            return answer;
    }
}